server:
  port: 8093
spring:
  application:
    name: mca-audit-trail-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ecs_platform
    username: ecs
    password: ecs_secret
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: mca-audit-trail-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8081/realms/ecs
